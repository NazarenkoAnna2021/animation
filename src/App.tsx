import React, { useRef, useState } from "react";
import { Animated, Text, View, StyleSheet, Button, SafeAreaView, TouchableOpacity } from "react-native";
import { ShowInfoButton } from "../modules/showInfoButton";

export const App = () => {
  return (
    <ShowInfoButton />
  );
}